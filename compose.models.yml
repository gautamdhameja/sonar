services:
  api:
    environment:
      SONAR_CHAT_API_KEY: ${SONAR_CHAT_API_KEY:-not-needed}
    models:
      chat_model:
        endpoint_var: SONAR_CHAT_BASE_URL
        model_var: SONAR_CHAT_MODEL

models:
  chat_model:
    model: ${SONAR_CHAT_MODEL:-hf.co/unsloth/gemma-4-E4B-it-GGUF:UD-Q4_K_XL}
    context_size: 8192
    runtime_flags:
      - "--temp"
      - "0.1"
